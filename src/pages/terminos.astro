---
import { BaseLayout } from "@layouts";
import { SiteFooter, SiteHeader, TermsContent } from "@components";
import { PAGE_META, organizationSchema } from "@config";
---

<BaseLayout meta={PAGE_META.terms} structuredData={organizationSchema}>
  <SiteHeader />
  <TermsContent />
  <SiteFooter />
</BaseLayout>
